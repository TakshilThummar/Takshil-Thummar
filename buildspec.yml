version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm install
  build:
    commands:
      - npm run build
      - echo "Build completed"
      - echo "Contents of dist directory:"
      - ls -al dist

artifacts:
  base-directory: dist
  files:
    - '**/*'
